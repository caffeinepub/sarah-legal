/**
 * Shared className for primary CTA buttons using the red accent theme.
 * Provides consistent styling across all landing page CTAs with proper hover states.
 */
export const primaryCtaClasses = 
  "bg-accent text-accent-foreground hover:bg-accent/90 transition-colors";
